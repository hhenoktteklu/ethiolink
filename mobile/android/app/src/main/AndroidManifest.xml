<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="ethiolink"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
            Cognito hosted-UI callback routing.

            `flutter_appauth` (via the upstream AppAuth-Android
            library) declares its own
            `net.openid.appauth.RedirectUriReceiverActivity` with the
            right launchMode / taskAffinity / theme to deliver the
            redirect back into the same task as the
            `AuthorizationManagementActivity` that initiated sign-in.
            The plugin's intent filter pulls the scheme from
            `${appAuthRedirectScheme}` — set to `com.ethiolink.app`
            in `android/app/build.gradle.kts` to match
            `COGNITO_REDIRECT_URI=com.ethiolink.app:/oauthredirect`
            in `mobile/env/dev.json`. The reverse-domain scheme is
            the OAuth-for-native-apps best practice from RFC 8252
            §7.1 — exactly one app on the device claims it, so the
            Cognito redirect cannot be intercepted by another app.

            We previously overrode the activity here with a
            full-URI intent filter + `tools:node="replace"`. That
            override dropped the plugin's stored-state plumbing and
            surfaced `AppAuth: No stored state - unable to handle
            response` after a successful Cognito redirect. The
            plugin's default contribution is now intentionally NOT
            replaced; the gradle `appAuthRedirectScheme` placeholder
            is sufficient.

            If the redirect URI ever changes, update BOTH the
            `appAuthRedirectScheme` value in
            `android/app/build.gradle.kts` AND the Cognito app
            client's allowed callback list — they must match.
        -->

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
