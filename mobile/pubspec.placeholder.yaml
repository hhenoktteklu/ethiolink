# SUPERSEDED — see `pubspec.yaml`.
#
# This file documented the intended Flutter dependency set for the
# Phase 0 scaffold milestone. The Phase 9 Track 3 scaffold ships a
# working `pubspec.yaml` alongside the real `lib/` tree; that file
# is the authoritative project manifest going forward.
#
# Kept in tree as a paper-trail anchor — `pubspec.placeholder.yaml`
# is the file the Phase 0 docs reference when explaining the
# Flutter rollout plan. Future cleanup commits may delete it; the
# real `pubspec.yaml` is the binding artifact.

name: ethiolink-superseded-placeholder
description: "Superseded by pubspec.yaml. See mobile/README.md."
publish_to: "none"
