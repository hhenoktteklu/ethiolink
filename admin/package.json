{
  "name": "@ethiolink/admin",
  "version": "0.0.0",
  "private": true,
  "description": "EthioLink admin dashboard (React + TypeScript). Initialized properly in Phase 5.",
  "scripts": {
    "dev": "echo \"Initialized in Phase 5\" && exit 0",
    "build": "echo \"Initialized in Phase 5\" && exit 0",
    "preview": "echo \"Initialized in Phase 5\" && exit 0",
    "lint": "echo \"Initialized in Phase 5\" && exit 0",
    "test": "echo \"Initialized in Phase 5\" && exit 0"
  }
}
